/*
   - list examples
   - each list item will have image, transformation url, image url to paste, apply button, copy url button

   initially make it working for only 1 type of transformation
   and then go for integration part
   and then add more transformations
 */

export default () => {
    return (
        <div>transformations goes here</div>
    );
}